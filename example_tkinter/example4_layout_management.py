import tkinter as tk


# def display_text():
#     label.config(text="Entered: " + entry.get())


window = tk.Tk()
window.title('My Window')
window.geometry('200x100')

# entry = tk.Entry(window)
# entry.pack()

# button = tk.Button(window, text='Display', command=display_text)
# button.pack()

# label = tk.Label(window, text='Hello, Tkinter!', bg='yellow', fg='black')
# label.pack()

window.mainloop()
